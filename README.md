# GLaDOS-TTS

Installer cuda 12.4
verif si installe : nvcc --version

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

install requirements

pip install nemo_toolkit[all] --no-deps
